<main class="content">

	<h1>Update {{patient.first_name}} {{patient.last_name}}:</h1>
	
	<form id="update-patient" action="/patient" method="post">
		First name: <input type="text" name="first_name" value="{{patient.first_name}}"><br>
		Last name: <input type="text" name="last_name" value="{{patient.last_name}}"><br>
		<!--
		Homeworld: <select name="homeworld" id="planet-selector"> 
			{{#each planets}}
				<option value="{{id}}">{{name}}</option>
			{{/each}}
		</select><br>
		-->
	</form>
	
	<button onclick="updatePatient({{patient.SSN}})">Update</button>
	<!--
	<script defer>selectPlanet({{person.homeworld}});</script>
	-->
</main>
